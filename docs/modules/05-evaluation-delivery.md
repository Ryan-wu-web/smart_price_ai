# Phase 5：离线评测、工程回归与本地交付

日期：2026-09-12。本模块收尾的是可运行本地版本的评测与交付，不把真实模型、真机或远端基础设施写成已验收。

## 目的与范围

用冻结样例测清楚已实现什么、哪里失败，并统一当前文档中“尚未接入聊天／Flutter／仍用随机Mock”的过期说法。按用户要求，主仓库不保留测试和临时脚本，但在仓库外保留独立可复现材料与原始失败日志。

## 修改文件

- `README.md`：当前能力、无模型启动、Flutter地址、真实模型单独验收、指标与局限。
- `docs/architecture.md`：统一为实际状态机、词法混合检索、完整品类硬过滤、偏好排序和文件／SQLite记忆，明确远端未接入。
- `docs/api.md`：评测入口、口语／硬软强度边界、已验证目录ID与null往返说明。
- `backend/.env.example`：仅更正模型密钥的适用范围注释，不改运行配置。
- `docs/evaluation.md`、`docs/acceptance.md`、本记录、`docs/modules/README.md`：可复现指标、失败归因、操作验收和模块状态。

生产逻辑最后改动仍是4E提交e4ea227；本模块未改推荐算法、冻结金标、依赖或锁文件。最终数据暴露的7个失败保留，不通过文案掩饰。

## 评测及复现

80条代理编写的合成样本，开发40、最终40，8组均衡。旧d02b1d0源码隔离重放：30次HTTP探针＋50个OpenAPI能力判断；新契约缺失，质量基线N/A。开发发现并修复会话null；原开发失败／评估器修正历史保留。最终首次测量33/40，购物场景23/25；详细分子分母及边界以[评测文档](../evaluation.md)为准。

外部目录 `D:\smart_price_ai-validation\smart-price-ai-evaluation-2026-09-12` 包含旧／新纯源码快照、冻结数据、原始及便携runner、结果与日志；同名ZIP用于本机交付，不上传主仓库。隔离重跑的development/final_test指标和失败列表与原始结果完全一致。模型调用0；P95首文本31.00ms仅属于本机确定性SSE。

## 实际验证

- Python当前契约回归11组345项通过（套件有重叠）；原Phase1检查31/32的过时契约失败日志保留，独立适配后32/32，不是跳过失败。
- 1B需--output的命令已纠正重跑29/29；最初命令exit2保留。Dart SSE26/26、购物20/20、识别上下文检查通过。
- Flutter analyze --no-pub：无问题；flutter build bundle --no-pub：exit0。未使用flutter_test，未真机或APK构建。
- 80条评测的原始与隔离重放结果一致；最终集7个失败仍存在。评估器exit0表示完成，不表示指标全通过。
- 10次真实loopback SSE校验终态和文本一致；测量后临时Uvicorn停止，没有部署。
- 收尾验证：50个后端app Python文件内存编译、变更Markdown本地链接／架构完整源码路径、Compose无变量插值配置解析、git diff --check通过；文档与复现包的敏感信息模式扫描未发现匹配，不等同于完整安全审计。
- 复现包完整性脚本同时检查缺失、变更与额外冻结文件；ZIP不含真实.env、数据库、私有会话或Python缓存。ZIP完整性、解压后核验和故意损坏的负向验证结果随包留档。
- 13:57便携SSE重跑中位36.10ms、P95 92.42ms；未覆盖首次数字，不宣称稳定模型性能。

## 风险与未完成

AnyIO TestClient有资源警告，不能把离线桩当真实模型调用；真实VLM/LLM、手机硬件与网络、偏好插件和报告导出待用户验收。当前NLU边界明显，中文数词、泛化口语及强度澄清未完成。Redis/PG只有评估和可选基础设施配置，未实现远端存储；没有多用户鉴权或多worker一致性。

复现包是仓库外材料，GitHub克隆不包含它；请单独备份。旧源码基线是事后重放，不是开发前预注册评测。合成最终集不是独立人工测试，结果不可外推真实用户分布。

## 回退与下一步

本模块只有文档与配置注释，回退不改变运行事实。若回退4E及更早生产代码，先备份会话和偏好，不能把新状态直接交给不支持的旧Schema。模块采用正常commit／push，不强推、不部署；推送成功以git ls-remote核验为准。

唯一下一步：按[验收手册](../acceptance.md)完成真实模型＋Flutter真机验收，不在已曝光最终集上继续调参。
