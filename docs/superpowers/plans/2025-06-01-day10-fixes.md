# Day 10 五大问题修复 — 实现计划

> **给代理工作者：** 必需子skill：使用 executing-plans 逐个任务执行此计划。步骤使用复选框（- [ ]）语法跟踪。

**目标：** 修复五个严重问题（图片加载、多目标识别、识别速度、AI上下文、Mock数据）
**架构：** 前后端联动修复，多目标识别全面重写，识别pipeline合并优化
**技术栈：** Flutter 3.24.5 + Dart, Python 3.13.9 + FastAPI, 火山引擎 Doubao-Seed-2.0-lite

---

## 批次1：后端识别Pipeline重构（合并调用 + 多目标API）

### 任务 1：重写 recognition.py（合并 VLM+LLM + 多目标识别）

**文件：**
- 修改：`backend/app/services/recognition.py`
- 修改：`backend/app/routers/recognize.py`
- 修改：`backend/app/models/schemas.py`

- [ ] **步骤 1：修改 schemas.py 增加多目标响应模型**

在 `backend/app/models/schemas.py` 末尾添加：

```python
class RecognizedObject(BaseModel):
    name: str
    brand: str = ""
    category: str
    color: str = ""
    bbox: dict[str, float] = Field(default_factory=dict)

class RecognizeMultiResponse(BaseModel):
    objects: list[RecognizedObject]
```

- [ ] **步骤 2：重写 recognition.py**

完整替换 `backend/app/services/recognition.py`：

```python
from app.core.llm_client import LLMClient
from app.core.prompt_engine import PromptEngine
from app.core.vlm_client import VLMClient
from app.models.schemas import RecognizeResponse, RecognizedObject, RecognizeMultiResponse


class RecognitionService:
    def __init__(
        self,
        vlm_client: VLMClient | None = None,
        llm_client: LLMClient | None = None,
    ):
        self.vlm_client = vlm_client or VLMClient()
        self.llm_client = llm_client or LLMClient()

    async def recognize(self, image_base64: str) -> RecognizeResponse:
        """单次调用主路径，失败时 fallback 到两阶段。"""
        # 主路径：直接调用 LLM，传入图片 + 识别 Prompt
        prompt = (
            "你是一位专业的商品识别专家。请观察图片中的商品，"
            "直接以 JSON 格式输出：name（商品名称）、brand（品牌，未知为空字符串）、"
            "category（品类）、color（主色调）、material（材质）、style（款式）。\n\n"
            "约束：\n"
            "- 如果无法识别品牌，brand 设为空字符串\n"
            "- category 必须是具体品类\n"
            "- 只输出 JSON，不要任何解释\n\n"
            "示例：\n"
            '{"name": "Nike Air Force 1 白色 42码", "brand": "Nike", "category": "运动鞋", "color": "纯白色", "material": "皮革", "style": "低帮板鞋"}'
        )
        messages = [
            {
                "role": "user",
                "content": [
                    {"type": "text", "text": prompt},
                    {"type": "image_url", "image_url": {"url": f"data:image/jpeg;base64,{image_base64}"}},
                ],
            }
        ]

        try:
            result = await self.llm_client.chat_json(messages, temperature=0.3)
            return self._parse_recognize_result(result)
        except Exception:
            # Fallback：两阶段识别
            return await self._recognize_two_stage(image_base64)

    async def _recognize_two_stage(self, image_base64: str) -> RecognizeResponse:
        """原始两阶段识别作为 fallback。"""
        description = await self.vlm_client.describe_image(image_base64)
        prompt = PromptEngine.recognize(description)
        messages = [{"role": "user", "content": prompt}]
        result = await self.llm_client.chat_json(messages, temperature=0.3)
        return self._parse_recognize_result(result)

    def _parse_recognize_result(self, result: dict) -> RecognizeResponse:
        return RecognizeResponse(
            name=result.get("name", ""),
            brand=result.get("brand", ""),
            category=result.get("category", ""),
            color=result.get("color", ""),
            material=result.get("material", ""),
            style=result.get("style", ""),
        )

    async def recognize_multiple(self, image_base64: str) -> RecognizeMultiResponse:
        """多目标识别：识别图中所有商品。"""
        prompt = (
            "你是一位专业的商品识别专家。请观察图片，识别图中所有独立的商品。\n"
            "对每件商品，输出以下字段：\n"
            "- name：商品名称\n"
            "- brand：品牌（无法识别写空字符串）\n"
            "- category：品类\n"
            "- color：主色调\n"
            "- bbox：检测框位置，格式为 {\"x\": 0-1, \"y\": 0-1, \"w\": 0-1, \"h\": 0-1}\n\n"
            "约束：\n"
            "- 只输出 JSON 数组，不要任何解释文字\n"
            "- 如果图中没有商品，输出空数组 []\n"
            "- x,y 是检测框左上角坐标，w,h 是宽度和高度（相对图片的归一化坐标 0-1）\n\n"
            "示例：\n"
            '[{"name": "怡宝纯净水 2.08L", "brand": "怡宝", "category": "饮料", "color": "透明", "bbox": {"x": 0.15, "y": 0.2, "w": 0.4, "h": 0.5}}]'
        )
        messages = [
            {
                "role": "user",
                "content": [
                    {"type": "text", "text": prompt},
                    {"type": "image_url", "image_url": {"url": f"data:image/jpeg;base64,{image_base64}"}},
                ],
            }
        ]

        try:
            result = await self.llm_client.chat_json(messages, temperature=0.3)
            if not isinstance(result, list):
                return RecognizeMultiResponse(objects=[])

            objects = []
            for item in result:
                if not isinstance(item, dict):
                    continue
                bbox = item.get("bbox", {})
                if isinstance(bbox, list) and len(bbox) >= 4:
                    bbox = {"x": bbox[0], "y": bbox[1], "w": bbox[2], "h": bbox[3]}
                elif not isinstance(bbox, dict):
                    bbox = {}
                objects.append(
                    RecognizedObject(
                        name=item.get("name", "未知商品"),
                        brand=item.get("brand", ""),
                        category=item.get("category", "未知"),
                        color=item.get("color", ""),
                        bbox=bbox,
                    )
                )
            return RecognizeMultiResponse(objects=objects)
        except Exception:
            return RecognizeMultiResponse(objects=[])
```

- [ ] **步骤 3：修改 recognize router 增加多目标路由**

在 `backend/app/routers/recognize.py` 中添加：

```python
@router.post("/recognize/multi", response_model=RecognizeMultiResponse)
async def recognize_multi(request: RecognizeRequest):
    try:
        service = RecognitionService()
        return await service.recognize_multiple(request.image_base64)
    except HTTPException:
        raise
    except Exception as e:
        logger.error(f"recognize_multi failed: {e}", exc_info=True)
        raise HTTPException(status_code=500, detail="Internal server error")
```

需要导入 `RecognizeMultiResponse` 和 `logger`。

- [ ] **步骤 4：运行后端测试**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai\backend
pytest tests/ -v
```

预期：25/25 PASS

- [ ] **步骤 5：提交**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai
git add backend/app/services/recognition.py backend/app/routers/recognize.py backend/app/models/schemas.py
git commit -m "feat(day10): merge VLM+LLM single call + add recognize_multi API"
```

---

## 批次2：前端多目标识别重写 + 图片加载修复

### 任务 2：重写 MultiObjectScreen

**文件：**
- 修改：`android-app/lib/screens/multi_object_screen.dart`
- 修改：`android-app/lib/services/api_service.dart`

- [ ] **步骤 1：ApiService 增加 recognizeMultiple 方法**

在 `api_service.dart` 中添加：

```dart
  Future<List<Map<String, dynamic>>?> recognizeMultiple(File image) async {
    if (!await NetworkChecker.isOnline()) {
      throw ApiException(ErrorMessages.noInternet);
    }
    final bytes = await image.readAsBytes();
    final base64Image = base64Encode(bytes);

    try {
      final response = await http.post(
        Uri.parse('$_baseUrl/api/v1/recognize/multi'),
        headers: {'Content-Type': 'application/json'},
        body: jsonEncode({'image_base64': base64Image}),
      ).timeout(const Duration(seconds: 120));

      if (response.statusCode == 200) {
        final data = jsonDecode(response.body) as Map<String, dynamic>;
        final list = data['objects'] ?? [];
        return (list as List<dynamic>).map((e) => e as Map<String, dynamic>).toList();
      } else {
        throw ApiException('识别失败: ${response.statusCode}');
      }
    } on TimeoutException catch (_) {
      throw ApiException(ErrorMessages.timeout);
    }
  }
```

- [ ] **步骤 2：重写 MultiObjectScreen**

完整替换 `multi_object_screen.dart`（约 200 行）：

关键结构：
- `initState`：调用 `_loadRecognitions()`
- `_loadRecognitions()`：显示加载遮罩 → 调用 API → 解析结果 → `setState`
- `_buildDetectionBox(obj, index)`：AnimatedBuilder ripple 动画 + 品牌色边框 + 标签 pill
- `_onBoxTap(obj)`：组装 RecognitionResult → 跳转 ResultScreen
- 空状态：Center 卡片 + search_off 图标 + "重新拍摄"按钮
- 加载状态：黑色遮罩 + 脉冲圆点 + "AI 正在识别..."

由于代码较长，执行时直接写入完整代码。

- [ ] **步骤 3：运行 Flutter 分析**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai\android-app
flutter analyze
```

预期：0 issues

- [ ] **步骤 4：提交**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai
git add android-app/lib/screens/multi_object_screen.dart android-app/lib/services/api_service.dart
git commit -m "feat(day10): MultiObjectScreen real API + detect boxes + ripple animations"
```

---

### 任务 3：ProductCard 图片加载修复

**文件：**
- 修改：`android-app/lib/widgets/product_card.dart`

- [ ] **步骤 1：替换图标为 Image.network**

将 `product_card.dart` 第32-44行替换为：

```dart
            ClipRRect(
              borderRadius: BorderRadius.circular(Constants.mediumRadius),
              child: product.imageUrl != null && product.imageUrl!.isNotEmpty
                  ? Image.network(
                      product.imageUrl!,
                      width: imageSize,
                      height: imageSize,
                      fit: BoxFit.cover,
                      loadingBuilder: (context, child, loadingProgress) {
                        if (loadingProgress == null) return child;
                        return Container(
                          width: imageSize,
                          height: imageSize,
                          color: const Color(0xFFE0E0E0),
                          child: const Center(
                            child: SizedBox(
                              width: 20,
                              height: 20,
                              child: CircularProgressIndicator(strokeWidth: 2),
                            ),
                          ),
                        );
                      },
                      errorBuilder: (_, __, ___) => _buildImagePlaceholder(imageSize),
                    )
                  : _buildImagePlaceholder(imageSize),
            ),
```

并添加辅助方法：

```dart
  Widget _buildImagePlaceholder(double size) {
    return Container(
      width: size,
      height: size,
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(Constants.mediumRadius),
        gradient: const LinearGradient(
          colors: [Color(0xFFE0E0E0), Color(0xFFF5F5F5)],
          begin: Alignment.topLeft,
          end: Alignment.bottomRight,
        ),
      ),
      child: const Icon(Icons.image, color: Constants.secondaryTextColor),
    );
  }
```

- [ ] **步骤 2：运行 Flutter 分析**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai\android-app
flutter analyze
```

预期：0 issues

- [ ] **步骤 3：提交**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai
git add android-app/lib/widgets/product_card.dart
git commit -m "fix(day10): ProductCard load image from network with error/loading fallback"
```

---

## 批次3：前端图片压缩 + AI 上下文优化 + Mock 数据

### 任务 4：前端图片压缩

**文件：**
- 修改：`android-app/lib/screens/home_screen.dart`

- [ ] **步骤 1：修改 pickImage maxWidth**

将第88行和第164行的 `maxWidth: 1200` 改为 `maxWidth: 800`：

```dart
picked = await picker.pickImage(source: source, maxWidth: 800);
```

和：

```dart
final picked = await picker.pickImage(source: ImageSource.camera, maxWidth: 800);
```

- [ ] **步骤 2：提交**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai
git add android-app/lib/screens/home_screen.dart
git commit -m "perf(day10): compress camera image maxWidth 1200→800"
```

---

### 任务 5：AI 上下文优化

**文件：**
- 修改：`backend/app/services/chat.py`
- 修改：`backend/app/core/prompt_engine.py`

- [ ] **步骤 1：修改 chat.py**

```python
SUMMARY_THRESHOLD = 8  # 4轮对话 = 8条消息
```

第91行改为：

```python
            return context[-6:]  # 保留3轮
```

- [ ] **步骤 2：修改 prompt_engine.py**

`chat_reply` 方法中：

```python
        ctx_text = "\n".join(
            [f"{'用户' if c['role'] == 'user' else '助手'}: {c['content']}" for c in context[-8:]]
        )
```

摘要 Prompt 改为：

```python
            summary_prompt = (
                "请对以下购物对话历史进行摘要，保留关键信息（用户关注的商品名称、"
                "价格、平台、核心需求、已做出的决策），控制在 100 字以内。\n\n"
                f"{history_text}\n\n"
                "只输出摘要内容，不要添加任何解释。"
            )
```

- [ ] **步骤 3：运行后端测试**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai\backend
pytest tests/ -v
```

预期：25/25 PASS

- [ ] **步骤 4：提交**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai
git add backend/app/services/chat.py backend/app/core/prompt_engine.py
git commit -m "perf(day10): AI chat context threshold 12→8, retain 4 rounds, richer summary"
```

---

### 任务 6：Mock 数据扩充（饮料/日用品）

**文件：**
- 修改：`backend/app/services/comparison.py`

- [ ] **步骤 1：在 base_items 末尾追加饮料/日用品数据**

追加约 15 条：

```python
            # 饮料
            {"name": "农夫山泉 天然水 550ml", "brand": "农夫山泉", "category": "饮料", "color": "透明", "price": 2.0},
            {"name": "怡宝 纯净水 555ml", "brand": "怡宝", "category": "饮料", "color": "透明", "price": 2.0},
            {"name": "可口可乐 经典口味 330ml", "brand": "可口可乐", "category": "饮料", "color": "红色", "price": 3.5},
            {"name": "雪碧 清爽柠檬味 330ml", "brand": "雪碧", "category": "饮料", "color": "绿色", "price": 3.5},
            {"name": "红牛 维生素功能饮料 250ml", "brand": "红牛", "category": "饮料", "color": "红色", "price": 6.5},
            {"name": "水溶C100 西柚味 445ml", "brand": "农夫山泉", "category": "饮料", "color": "透明", "price": 4.5},
            {"name": "元气森林 气泡水 480ml", "brand": "元气森林", "category": "饮料", "color": "透明", "price": 5.0},
            # 零食
            {"name": "乐事 原味薯片 70g", "brand": "乐事", "category": "零食", "color": "黄色", "price": 6.5},
            {"name": "卫龙 大面筋辣条 65g", "brand": "卫龙", "category": "零食", "color": "红色", "price": 3.5},
            {"name": "三只松鼠 每日坚果 25g", "brand": "三只松鼠", "category": "零食", "color": "混合", "price": 5.0},
            # 日用品
            {"name": "维达 抽纸 3层130抽", "brand": "维达", "category": "日用品", "color": "白色", "price": 3.5},
            {"name": "蓝月亮 洗衣液 1kg", "brand": "蓝月亮", "category": "日用品", "color": "蓝色", "price": 25.0},
            {"name": "云南白药 牙膏 100g", "brand": "云南白药", "category": "日用品", "color": "白色", "price": 18.0},
            {"name": "舒肤佳 沐浴露 720ml", "brand": "舒肤佳", "category": "日用品", "color": "白色", "price": 28.0},
            {"name": "海飞丝 洗发水 400ml", "brand": "海飞丝", "category": "日用品", "color": "蓝色", "price": 35.0},
```

- [ ] **步骤 2：验证数据量**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai\backend
python -c "from app.services.comparison import MockDataSource; m = MockDataSource(); print(f'Total: {len(m.MOCK_PRODUCTS)}')"
```

预期：约 450+ 条

- [ ] **步骤 3：提交**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai
git add backend/app/services/comparison.py
git commit -m "data(day10): add 15 beverage/snack/daily items to mock data"
```

---

## 批次4：构建验证与收尾

### 任务 7：全量验证

- [ ] **步骤 1：后端测试**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai\backend
pytest tests/ -v
```

预期：25/25 PASS

- [ ] **步骤 2：Flutter 分析**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai\android-app
flutter analyze
```

预期：0 issues

- [ ] **步骤 3：Flutter Release 构建**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai\android-app
flutter build apk --release
```

预期：构建成功

- [ ] **步骤 4：更新 handoff-prompt.md**

追加 Day 10 修复记录。

- [ ] **步骤 5：最终提交**

```bash
cd C:\Users\Lenovo\Desktop\super_test\smart-price-ai
git add docs/handoff-prompt.md
git commit -m "docs(day10): update handoff-prompt with all 5 fixes"
```

---

## 自检清单

- [ ] 规格覆盖：5 个问题都有对应任务
- [ ] 占位符扫描：无 TBD/TODO
- [ ] 类型一致性：RecognizedObject/RecognizeMultiResponse 前后端字段一致
- [ ] 错误处理：空结果/超时/加载失败都有兜底
